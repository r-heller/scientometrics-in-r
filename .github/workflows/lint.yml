name: Lint

on:
  push:
    branches: [main]
    paths:
      - "R/**"
      - "scripts/**"
      - "chapters/**"
  pull_request:
    branches: [main]
    paths:
      - "R/**"
      - "scripts/**"
      - "chapters/**"
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: "4.4.1"
          use-public-rspm: true

      - name: Install dependencies
        run: |
          install.packages(c("lintr", "styler"))
        shell: Rscript {0}

      - name: lintr
        run: |
          library(lintr)
          lints <- lint_dir("R")
          if (length(lints) > 0) {
            print(lints)
            stop("lintr found issues. See above.")
          }
          cat("lintr: all clear\n")
        shell: Rscript {0}

      - name: styler
        run: |
          library(styler)
          result <- style_dir("R", dry = "fail")
          if (any(!result$changed)) {
            cat("styler: all files already styled\n")
          }
        shell: Rscript {0}

      - name: Install Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Quarto check
        run: quarto check
